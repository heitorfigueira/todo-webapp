import { Component } from '@angular/core';

@Component({
  selector: 'todo-web-authentication-signin',
  template: `<router-outlet></router-outlet>`,
})
export class AuthenticationSigninComponent {
  title = 'authentication-signin';
}
